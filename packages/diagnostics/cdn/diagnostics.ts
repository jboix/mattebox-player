/**
 * The CDN entry: the package's public surface behind the
 * `matteboxPlayerDiagnostics` global. Importing registers <mbx-diagnostics>,
 * the same as the package.
 */
export * from '../src/index.js';
